name: "SlayerRewards"
main: "${project_group}.slayerrewards.SlayerRewards"
version: "${project_version}"
extension:
  depend: []
git:
  provider: github
  owner: ${git_org_name}
  repository: ${git_org_repository}
